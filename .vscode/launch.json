{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug MiniFS (gcc + gdb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/mini_fs.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "stopAtEntry": false,
            "preLaunchTask": "Build MiniFS",
            "externalConsole": false
        }
    ]
}
